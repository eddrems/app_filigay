/**
 * Copia este archivo como `config.js` y completa tus credenciales de Supabase.
 * Dashboard: https://supabase.com/dashboard → Project Settings → API
 */
export const supabaseConfig = {
  // Solo la URL del proyecto (sin /rest/v1)
  url: 'https://TU_PROYECTO.supabase.co',
  // Settings → API → "anon" / "Publishable" (JWT eyJ…)
  anonKey: 'TU_ANON_KEY_PUBLICA',
};
