/**
 * Plantilla — el proyecto usa js/config.js (versionado para deploy).
 * Override local opcional: js/config.local.js (gitignored)
 */
export const supabaseConfig = {
  url: 'https://TU_PROYECTO.supabase.co',
  anonKey: 'TU_ANON_KEY_PUBLICA',
};
